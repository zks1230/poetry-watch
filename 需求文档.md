# 诗词手表 App 需求文档

> 版本：v1.1
> 日期：2026-09-26
> 平台：Android Wear OS（适配圆屏 + 方屏）
> 技术栈：原生 Kotlin + Jetpack Compose for Wear OS

---

## 版本记录

| 版本 | 日期 | 内容 |
|---|---|---|
| v1.0 | 2026-09-19 | 初版需求 |
| v1.1 | 2026-09-26 | ① 新增**首次启动屏幕适配向导**（汉克米应用商店风格：先选表盘形状 + 安全区，带实时预览）；② **修复「点开始检测直接停止运行」**（根因见下）；③ 检测页/详情页异常收敛 + 崩溃日志页 + 诊断信息页；④ 检测题支持无标点诗词；⑤ 老安卓兼容（java.time 脱糖、低版本不调用 isScreenRound）；⑥ 新增 13 个自动化测试（含 5 个在电脑上真实渲染界面的回归测试） |

### 「点开始检测直接停止运行」根因（v1.1 已修复）

v1.0 的 `QuizScreen` 在 `Column { ... }`（inline composable lambda）里用**提前返回**做分支：

```kotlin
if (questions.isEmpty()) {
    Text("该诗词未生成可检测的句子（需含标点分隔）。")
    return@Column   // ← 崩溃点
}
```

一进检测页时题目还在异步读取中，因此**第一次组合必然走这一分支**，
在 Compose 运行时抛出：

```
java.lang.ArrayIndexOutOfBoundsException: Index -5 out of bounds for length 0
    at androidx.compose.runtime.SlotTableKt.key(SlotTable.kt:3522)
    ...
    at androidx.compose.runtime.ComposerImpl.endRoot(Composer.kt:1483)
```

线程是主线程 → 进程直接被杀 → 系统弹「已停止运行」。
已用 Robolectric 复现并验证：**inline lambda 内 `return@Column` 会崩，
改成 `if/else` 或 `when` 分支即正常**（探针 A 失败 / 探针 B 通过）。

修复方式：
1. `QuizScreen` 全量改为 `when { ... }` 分支 + 私有子 Composable，不再有提前返回；
2. `DetailScreen` 的 `return@Scaffold` 同样改为 `if/else`；
3. 全仓库已无 `return@Column/Row/Box/LazyColumn` 之类写法（可用 grep 复查）。

---


## 一、产品定位

一款运行于安卓智能手表的诗词学习应用，核心能力为 **诗词背诵 + 每日推荐**，让用户在手腕上即可完成"看诗—背诗—检测—复习"的完整学习闭环。先期聚焦手表端，手机端作为后续扩展阶段。

## 二、目标用户

- 喜欢古典诗词、希望利用碎片时间背诵诗词的用户
- 已拥有安卓 Wear OS 智能手表（圆屏或方屏均可）

## 三、核心功能

### 3.1 每日推荐
- 每日首页展示一首"今日推荐"诗词
- 推荐算法 = **节气/节日匹配 + 用户背诵进度** 双因子结合
  - 节气因子：根据当前农历节气/节日选取应景诗词（如中秋推荐《水调歌头》、清明推荐《清明》）
  - 进度因子：优先推荐尚未学习、或长期未复习的诗词，巩固记忆
- 用户可一键将推荐诗词加入背诵计划

### 3.2 诗词浏览
- 列表浏览全部诗词（按**册数/年级**、朝代、作者、分类筛选）
- 诗词详情页内容：**原文 + 译文**（仅此两项，保持小屏信息精简）
  - 原文：诗题、作者、朝代、正文
  - 译文：现代汉语翻译
  - 附注：所属学段册数（如"人教版·七年级上册"）

### 3.3 背诵管理
- **手动标记**：用户可将诗词标记为 "未学 / 学习中 / 已掌握" 三种状态
- **检测模式**：用户主动进入检测，系统通过接龙选择题进行背诵自测
  - 接龙选择题：给出诗中某句的上半句，用户从 4 个选项中选择正确的下半句
  - 答对：加深记忆；答错：标记为"需复习"
- 用户自行决定何时手动标记、何时进入检测模式

### 3.4 诗词内容管理
- **内置精选库**：App 安装即包含经典诗词集（如唐诗 300 首精选、宋词精选），离线可用
- **联网更新**：支持联网拉取扩充诗词库（按分类/朝代增量下载）
- **用户自录入**：用户在手表端可手动录入自己的诗词文本（诗题、作者、正文、译文）

### 3.5 主题切换
- 多套主题可切换，至少包含：
  - **极简风**（默认）：深色背景 + 浅色文字，省电、清晰
  - 其他主题：例如古典宣纸风、四季时令风等
- 主题切换实时生效

## 四、交互设计

### 4.1 核心交互
- **滑动**：上下滑动翻页 / 滚动列表；左右滑动切换详情页（原文 ↔ 译文）
- **点击**：点击列表项进入详情；点击选项作答检测题
- 不引入语音、表冠等复杂交互（保持首版简洁）

### 4.2 屏幕适配
- **首次启动向导（v1.1 新增，汉克米风格）**：安装后第一次打开 App，先进入「屏幕适配」两步向导
  1. 选择屏幕形状：自动识别 / 圆形表盘 / 方形表盘 / 长方形表盘
  2. 选择安全区大小：紧凑 / 标准 / 宽松，并给出**实时文字裁切预览**
  - 选择结果写入 DataStore，之后所有页面按该方案排版；设置页可随时重新适配
- **圆屏适配**：内容收进中心方形区域，边缘内容自动向中心收缩，避免被裁切
- **方屏适配**：使用标准居中布局，最大化利用屏幕空间
- **安全区**：统一的内缩档位，用户发现文字被表壳吃掉时可调大
- 字体、间距通过 `BoxWithConstraints` / 屏幕尺寸按需自适应缩放

### 4.3 稳定性（v1.1）
- 任何页面出现异常都不得直接「停止运行」：
  - 检测页异常 → 页面内显示原因 + 重试/返回
  - 数据流异常 → 降级为空数据
- 崩溃日志：`Thread.setDefaultUncaughtExceptionHandler` 记录堆栈到应用私有目录，
  下次启动弹出「上次运行崩溃了」页（可复制），设置 → 诊断信息中亦可查看设备信息与崩溃日志


## 五、界面结构（信息架构）

```
App
├── 首次启动向导（v1.1）
│   └── 屏幕适配：形状选择 → 安全区选择（实时预览）
├── 首页（今日推荐）
│   ├── 推荐诗词卡片
│   └── 快捷入口：开始检测 / 浏览诗词库 / 我的背诵
├── 诗词库
│   ├── 全部诗词列表
│   └── 详情页（原文 / 译文）
├── 背诵中心
│   ├── 我的背诵计划（未学/学习中/已掌握 三栏）
│   └── 检测模式（接龙选择题）
├── 内容管理
│   ├── 更新诗词库（联网）
│   └── 录入新诗词
└── 设置
    ├── 屏幕适配（重新适配）
    ├── 主题切换
    └── 诊断信息 / 崩溃日志（v1.1）
```

## 六、数据模型

### 6.1 Poem（诗词）
| 字段 | 类型 | 说明 |
|---|---|---|
| id | String | 唯一 ID（UUID） |
| title | String | 诗题 |
| author | String | 作者 |
| dynasty | String | 朝代（唐/宋/...） |
| content | String | 原文（多句以 \n 分隔） |
| translation | String | 译文 |
| level | String | 学段：初中 / 高中（当前仅初中） |
| grade | String | 所在年级册数，如"七年级上册"（人教版） |
| version | String | 教材版本：人教（当前默认）/ 苏教 / 北师大 等 |
| category | String | 分类（节气/节日/题材等标签） |
| source | String | 来源：builtin / remote / user |
| createdAt | Long | 录入时间戳 |

### 6.2 Progress（学习进度）
| 字段 | 类型 | 说明 |
|---|---|---|
| poemId | String | 关联诗词 ID |
| status | String | not_started / learning / mastered |
| lastReviewAt | Long? | 最后复习时间戳 |
| reviewCount | Int | 累计检测次数 |
| correctCount | Int | 累计答对次数 |

### 6.3 DailyRecommendation（每日推荐记录）
| 字段 | 类型 | 说明 |
|---|---|---|
| date | String | yyyy-MM-dd |
| poemId | String | 推荐诗词 ID |
| reason | String | 推荐理由（节气匹配 / 进度巩固） |

## 七、技术方案

### 7.1 技术栈
- 语言：Kotlin
- UI：声明式 UI（Compose）
- 数据持久化：Room 数据库（SQLite 封装）
- 异步：Kotlin Coroutines + Flow
- 依赖注入：Hilt
- 网络：Retrofit + OkHttp（用于联网更新诗词库）
- 偏好：DataStore（主题切换记忆）

### 7.1.1 双版本架构（纯安卓 + Wear OS）
> 需求演进：除 Wear OS 外，还需兼容**纯安卓手机、老安卓、华强北智能手表**。

采用 `productFlavors` 双版本，**数据/逻辑/ViewModel 共享**（都在 `src/main`），仅 UI 各自实现：

| 版本 | flavor | 最低 SDK | UI 方案 | 适配目标 |
|---|---|---|---|---|
| **wear** | `wear` | 30 | Wear OS Compose（`ScalingLazyColumn`、TimeText、表冠友好组件） | 新版 Wear OS 3+ 圆屏/方屏手表 |
| **universal** | `universal` | 21 | 标准 Jetpack Compose Material3 | 纯安卓手机、老安卓（5.1+）、华强北手表（圆/方屏） |

- 两版本生成两个 APK：`app-wear.apk`、`app-universal.apk`
- 主题切换在 universal 版完整生效（Material3 全套配色映射）；wear 版应用背景色主题
- 圆屏/方屏自适应：universal 版通过内容内边距保证边缘文字不被裁切；wear 版用 Wear 组件内置的圆形适配

### 7.2 项目结构
```
app/
├── src/main/                                // 共享层
│   ├── java/com/example/poetrywatch/
│   │   ├── PoetryApp.kt                     // Application（Hilt）
│   │   ├── ui/
│   │   │   ├── Routes.kt                    // 导航路由（共享）
│   │   │   ├── MainViewModel.kt             // 主题 + seed 导入（共享）
│   │   │   ├── theme/                       // 主题定义（共享）
│   │   │   └── home|library|.../*ViewModel.kt  // 各页 ViewModel（共享）
│   │   ├── data/
│   │   │   ├── db/                          // Room 实体 + DAO
│   │   │   ├── repository/                  // 仓库层
│   │   │   ├── seed/                        // 内置诗词 seed 导入
│   │   │   └── preferences/                 // DataStore
│   │   ├── domain/                          // 推荐算法
│   │   └── di/                              // Hilt 模块
│   ├── assets/seed_poems.json               // 内置诗词数据
│   └── res/                                 // 资源
├── src/wear/java/.../ui/                    // wear 版 UI（Wear Compose）+ MainActivity
└── src/universal/java/.../ui/               // universal 版 UI（Material3）+ MainActivity
```

### 7.3 推荐算法实现要点
- 节气匹配：内置节气-诗词映射表，按当前日期查农历节气
- 进度排序：从 Progress 表筛选 `status != mastered` 或 `lastReviewAt` 超过阈值（如 7 天）的诗词
- 综合评分：节气匹配 +100，进度未学 +50、学习中 +30、超期未复习 +40，取最高分推荐

## 八、非功能需求

- **性能**：冷启动 ≤ 2s，列表滑动 60fps
- **存储**：内置诗词库体积控制在 5MB 以内
- **电池**：默认深色主题以省电；非活跃时及时释放资源
- **离线**：无网络时，内置库 + 已下载内容 + 自录入内容全部可用

## 九、开发阶段划分

| 阶段 | 内容 | 状态 |
|---|---|---|
| P0 | 项目骨架 + 主题系统 + 圆屏/方屏适配 | ✅ 已完成（含首次启动屏幕适配向导） |
| P1 | Room 数据库 + 内置诗词 seed + 诗词库浏览 | ✅ 已完成（内置 70 首） |
| P2 | 每日推荐算法 + 首页 | ✅ 已完成 |
| P3 | 背诵管理（手动标记 + 接龙检测） | ✅ 已完成（含防闪退改造） |
| P4 | 联网更新 + 用户自录入 | 🚧 自录入已完成，联网更新预留 |
| P5 | 设置 + 收尾打磨 | ✅ 主题切换 / 屏幕适配 / 诊断页已完成 |

## 十、附录

### 内置诗词范围（内容规划）
- **优先：初中人教版全量必背诗词**：收录部编版（人教版）初中语文课本 六册 全部必背古诗词（含课内 + 课外诵读），覆盖：
  - 七年级上册 / 七年级下册
  - 八年级上册 / 八年级下册
  - 九年级上册 / 九年级下册
- 每首诗词标注**所在册数**，便于按年级筛选复习
- **后续扩展（预留字段）**：高中（人教版）古诗词、其他教材版本（苏教/北师大）的诗词，按需联网增量补充

### 推荐映射素材
- 节气表：立春、雨水、惊蛰...共 24 节气
- 主要节日：春节、元宵、清明、端午、中秋、重阳、除夕
- 对应应景诗词示例（内置库中选用）：中秋→《水调歌头·明月几时有》、清明→《清明》
