# 诗词手表 App · 真机验证清单

> 对应版本：v1.1（首次启动屏幕适配向导 + 检测页闪退修复 + 崩溃日志）
> 更新日期：2026-09-26

---

## 一、安装哪个包

| 包 | 路径 | 适用 |
|---|---|---|
| **universal（优先）** | `release/poetrywatch-universal-debug.apk` | 华强北手表、老安卓、手机（Android 5.1+） |
| wear | `release/poetrywatch-wear-debug.apk` | 正版 Wear OS 3+ 手表 |

> wear 版在清单里声明了 `android.hardware.type.watch`，华强北手表装了可能直接提示"不兼容"，
> 所以**先装 universal**。

安装方式（任选其一）：

1. 把 APK 拷到手表存储里，用手表上的文件管理器点开安装；
2. 数据线连电脑：`adb install -r release\poetrywatch-universal-debug.apk`

覆盖安装即可，不用卸载（诗词库、学习进度会保留）。

---

## 二、必测项（按顺序做一遍）

| # | 操作 | 期望结果 |
|---|---|---|
| 1 | 卸载后重新安装，打开 App | 出现「**屏幕适配 1/2**」，列出 自动识别/圆形表盘/方形表盘/长方形表盘 |
| 2 | 选一个形状 → 下一步 | 出现「屏幕适配 2/2」，上面有**文字裁切预览**，下面 紧凑/标准/宽松 |
| 3 | 选「宽松」→ 开始使用 | 进入首页，且**页面四周文字不被表壳裁掉** |
| 4 | 首页 | 有「★ 今日推荐」卡片 + 诗词库/背诵中心/内容管理/设置 四个入口 |
| 5 | 诗词库 | 按册数分组，共 70 首左右 |
| 6 | 点任意诗词 → **开始检测（接龙）** | **能出题、能点选项、能看「检测完成 答对 x/y」**（这是上次闪退的位置，重点验证） |
| 7 | 检测里选错一次并走完流程 | 能正常结束，返回后该诗词状态变成「学习中」 |
| 8 | 背诵中心 | 未学/学习中/已掌握 三栏可切换，能点进详情 |
| 9 | 设置 → 主题 | 切「宣纸/春/夏/秋/冬」立刻变色 |
| 10 | 设置 → 屏幕适配 | 显示当前形状和安全区，点进去可重新适配 |
| 11 | 设置 → 诊断信息 / 崩溃日志 | 能看到安卓版本、机型、屏幕尺寸、是否圆屏、诗词数量 |

---

## 三、这次到底修了什么（「点开始检测就停止运行」）

根因不是数据问题，而是 **Compose 的提前返回写法**：

- v1.0 的检测页里写的是 `if (题目为空) { 提示文字; return@Column }`；
  `return@Column` 位于 `Column { ... }`（inline composable lambda）内部，
  一进检测页题目还没读完、必然走这个分支，Compose 运行时直接抛
  `ArrayIndexOutOfBoundsException: Index -5 out of bounds for length 0`，
  主线程崩溃 → 系统提示「已停止运行」。
- 已在电脑上用 Robolectric 复现：同样的提前返回写法必崩（探针 A 失败），
  改成 `if/else` 立刻正常（探针 B 通过）；现在检测页/详情页已全部改成分支结构，
  仓库里再无此类写法。
- 这次补了 13 个自动化测试（`app/src/test`），其中 5 个会**真实渲染**检测页、
  走完「出题 → 选答案 → 下一题」流程，以后再改坏会立刻暴露。

---

## 四、万一还是崩溃 / 有问题

新版**不会再只弹「已停止运行」**：

1. **重新打开 App**，会先出现「**上次运行崩溃了**」页面，里面有完整的错误堆栈，
   可以点「复制并继续使用」，或者**直接截图发我**；
2. 也可以在 **设置 → 诊断信息 / 崩溃日志** 里看到同样的信息 + 设备信息；
3. 想反馈别的毛病（文字太小、按钮点不到、列表被裁），请说明：
   - 手表型号 / 安卓版本（诊断页里有）
   - 出问题的页面名称
   - 一张屏幕照片（最直观）

---

## 五、开发者本地构建（Windows）

本机环境已装好（放在 `C:\dev`，不走系统 PATH）：

```powershell
$env:JAVA_HOME='C:\dev\jdk17'
$env:ANDROID_HOME='C:\dev\android-sdk'
$env:ANDROID_SDK_ROOT='C:\dev\android-sdk'

# universal 包
C:\dev\gradle-8.5\bin\gradle.bat --init-script C:\dev\init.gradle `
  '-Dorg.gradle.jvmargs=-Xmx3072m' -p C:\Users\admin\Documents\watch `
  :app:assembleUniversalDebug --console=plain

# wear 包
C:\dev\gradle-8.5\bin\gradle.bat --init-script C:\dev\init.gradle `
  '-Dorg.gradle.jvmargs=-Xmx3072m' -p C:\Users\admin\Documents\watch `
  :app:assembleWearDebug --console=plain

# 自动化测试（检测题生成 + 检测页真实渲染）
C:\dev\gradle-8.5\bin\gradle.bat --init-script C:\dev\init.gradle `
  -p C:\Users\admin\Documents\watch :app:testUniversalDebugUnitTest --console=plain
```

产物位置：

- `app/build/outputs/apk/universal/debug/app-universal-debug.apk`
- `app/build/outputs/apk/wear/debug/app-wear-debug.apk`

> `--init-script C:\dev\init.gradle` 只是把依赖仓库换成阿里云镜像加速，不影响产物。
